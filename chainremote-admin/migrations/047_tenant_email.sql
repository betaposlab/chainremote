-- 047: 대리점 연락 이메일 (2026-08-15)
--
-- 대리점 등록·정보 화면에 이메일 칸이 없었다. 세금계산서·구독 안내·비밀번호 안내처럼
-- 전화로 못 보내는 것들이 전부 이메일로 가는데, 받을 주소를 어디에도 안 적어 뒀다.
--
-- 로그인 아이디(users.email)와는 다른 값이다 — 그쪽은 'chang' 처럼 이메일 형식이 아닌
-- 아이디도 쓰고 있고, 청구는 대개 경리 담당자 주소로 간다. 그래서 회사 정보에 따로 둔다.
ALTER TABLE tenants ADD COLUMN IF NOT EXISTS contact_email text;
